from setuptools import setup

setup(name='gym_snake_classic',
      version='0.0.1',
      install_requires=['gym',
                  'pygame',
                  'numpy',
                  'matplotlib',
                  'pillow'
                  ]  # And any other dependencies foo needs
)